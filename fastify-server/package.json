{
  "name": "fastify-server",
  "version": "1.0.0",
  "description": "Fastify REST API for performance comparison",
  "main": "src/server.ts",
  "scripts": {
    "dev": "ts-node src/server.ts",
    "build": "tsc",
    "start": "ts-node src/server.ts"
  },
  "dependencies": {
    "fastify": "^4.24.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  }
}
